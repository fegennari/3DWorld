Tile Occlusion?

Openmp when another process is running?

Smoke texture step size
Indirect lighting normal/direction

Tree space colonization
Tree branch/trunk bark bump maps

Fix ship collisions

More grass/plant types
Leaves on ground (as texture)

Parallax mapping

Tighter textures to reduce fill rate by making the texture tighter (tree leaves/billboards)

Removed deprecated OpenGL calls:
Use triangles instead of quads (indexed draw for individual/isolated quads)
Remove immediate mode calls
- Billboard render manager
Remove non-shader rendering
